<h1>New Review Form <%= @restaurant.name %> </h1>

<%= form_for([@restaurant, @review]) do |f| %>

  <%= f.label :Review %>
  <%= f.text_area :body %>

  <%= f.label :rating %>
  <%= f.text_field :rating %>

  <%= f.submit "Add New Review" %>

<% end %>
